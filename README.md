# hivie.tn

Personal portfolio website. Built with TanStack Start.

## Development

### Prerequisites

- Node.js 20+
- pnpm

### Installation and running

```bash
pnpm install
pnpm dev
```

### Deployment

Deployment happens automatically via GitHub Actions when pushing to the `main` branch. `--skip-deploy` flag in the commit message can be used to skip deployment.
